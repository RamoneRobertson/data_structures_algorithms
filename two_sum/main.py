def twoSum(self, nums: List[int], target: int) -> List[int]:
    # inputs - list of unique numbers (list) : target num

    # loop through the list
    # Evaluate what the complement is based on the target and current num in the iteration
    # on each iteration check if the total adds up to the target
    # return the indices in a list
    pass
